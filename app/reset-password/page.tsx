'use client';
import {useState,type FormEvent} from 'react';
import {KeyRound} from 'lucide-react';
import {createClient} from '@/lib/supabase/client';

export default function ResetPassword(){const [password,setPassword]=useState('');const [busy,setBusy]=useState(false);const [message,setMessage]=useState('');const [error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setBusy(true);setError('');const {error}=await createClient().auth.updateUser({password});setBusy(false);if(error){setError(error.message);return;}setMessage('Senha alterada. Você já pode entrar.');setTimeout(()=>window.location.assign('/'),1200);};return <main className="access-page reset-only"><section className="access-panel"><form className="access-card" onSubmit={submit}><div className="access-icon"><KeyRound size={24}/></div><p className="eyebrow">NOVA SENHA</p><h2>Crie uma nova senha</h2><p className="access-description">Escolha uma senha com pelo menos 6 caracteres.</p><div className="access-fields"><label>Nova senha<input type="password" autoComplete="new-password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label></div>{error&&<p className="access-feedback error">{error}</p>}{message&&<p className="access-feedback success">{message}</p>}<button className="primary access-action" disabled={busy}>{busy?'Salvando...':'Salvar nova senha'}</button></form></section></main>;}
