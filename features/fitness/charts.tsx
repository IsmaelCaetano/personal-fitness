'use client';
import {Area,AreaChart,CartesianGrid,ResponsiveContainer,Tooltip,XAxis,YAxis} from 'recharts';
export function FitnessChart({points,label,unit}:{points:{date:string;value:number}[];label:string;unit:string}){return <div className="fitness-chart" role="img" aria-label={`${label}. ${points.map(p=>`${p.date}: ${p.value.toFixed(1)} ${unit}`).join('; ')}`}><ResponsiveContainer width="100%" height={220}><AreaChart data={points} margin={{left:-18,right:12,top:10,bottom:0}}><defs><linearGradient id="chart-fill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="var(--primary)" stopOpacity={.22}/><stop offset="100%" stopColor="var(--primary)" stopOpacity={0}/></linearGradient></defs><CartesianGrid vertical={false} stroke="var(--border)" strokeDasharray="4 5"/><XAxis dataKey="date" axisLine={false} tickLine={false} tick={{fill:'var(--muted-foreground)',fontSize:12}} dy={8}/><YAxis axisLine={false} tickLine={false} tick={{fill:'var(--muted-foreground)',fontSize:12}} domain={[0,'auto']}/><Tooltip contentStyle={{background:'var(--popover)',border:'1px solid var(--border)',borderRadius:10,color:'var(--foreground)'}} formatter={(value)=>[`${Number(value).toLocaleString('pt-BR',{maximumFractionDigits:1})} ${unit}`,label]}/><Area name={label} type="monotone" dataKey="value" stroke="var(--primary)" fill="url(#chart-fill)" strokeWidth={2.5} dot={{r:4,fill:'var(--primary)',stroke:'var(--card)',strokeWidth:3}} activeDot={{r:6}}/></AreaChart></ResponsiveContainer></div>;}
